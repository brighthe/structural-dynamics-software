# 投标算例补充材料清单（请陈国海老师协助，响应谱报告核对后）

陈老师好：

您前面提供的《平稳随机振动.docx》已经覆盖标书中**全部平稳 + 非平稳随机振动**相关指标（共 2 个 ★ 功能 + 15 个 ▲ 性能指标）。您后续提供的《响应谱分析算例测试报告.docx》也已经能够补上响应谱（反应谱）分析算例的主体材料，非常感谢。

我们初步核对后，响应谱报告中已经包含 **NRL、SRSS、ABS、CQC** 等响应谱组合算例，并给出了开发程序与 NASTRAN H5 结果的对比、关键峰值误差、包络曲线误差和验证结论。该报告可作为标书《八、算例测试报告》中“响应谱分析算例测试报告”的主要补充材料。

目前只剩两个小点需要请您确认或补充，主要是为了让报告和招标指标完全一一对应。

---

## 一、【请确认 / 必要时补充】GRP 叠加法算例

招标指标中要求响应谱分析支持 **SRSS、NRL、GRP、CQC** 四种叠加法。

现有《响应谱分析算例测试报告.docx》中明确出现的是：

1. NRL 叠加法算例；
2. SRSS 叠加法算例；
3. ABS 叠加法算例；
4. CQC 叠加法算例。

目前报告中没有检索到 **GRP** 字样。因此请帮忙确认：

- 报告中的 **ABS 叠加法算例** 是否本来就是要对应招标里的 **GRP 分组法**，只是名称写成了 ABS；
- 如果不是，请补一个 **GRP 叠加法响应谱算例**，最好沿用现有报告格式，包含模型与工况说明、组合方法参数、开发程序与 NASTRAN 对比结果、误差和验证结论。

若需要补 GRP，最小内容建议如下：

- 模型文件及模型特征；
- 反应谱输入、阻尼、模态阶数；
- GRP 分组准则或分组参数；
- 关键节点峰值结果；
- 与 NASTRAN 或基准程序的误差对比；
- 一句明确验证结论。

---

## 二、【请确认 / 必要时补充】多点激励说明或算例

招标指标中还有一项：**响应谱分析支持单点、多点激励施加**。

现有报告中已经明确出现“单点支座激励”，并且各算例表中写有 `SPC 约束集、SUPORT 支座方向、DLOAD 响应谱输入；3 组 TABLED1 谱表`。但报告正文里暂时没有明确写出“多点激励”“多支座激励”或“多方向激励”。

请帮忙确认：

- 现有这些响应谱算例是否已经包含多支座、多方向或多激励点输入；
- 如果包含，建议在报告中补一两句话明确说明“本算例通过哪些支座/方向/谱表组织多点或多方向响应谱输入”；
- 如果不包含，请补一个最小多点激励响应谱算例。

多点激励算例最小内容建议如下：

- 支座或激励点分组；
- 输入方向和反应谱设置；
- 多点输入下的模态参与或等效输入处理；
- 关键节点峰值响应；
- 与单点结果或 NASTRAN 结果的对比；
- 验证结论。

---

## 三、【可选，建议补充】三个小算例

下面三项标书里已有文字说明，但缺直接算例；如方便，各补一个**最小平稳随机振动算例**（与 NASTRAN 或基准结果对比即可），证明力度会更强：

1. **杆单元 / 梁单元**（CROD / CBAR / CBEAM 等）；
2. **刚性连接 / 刚性多点约束**（RBAR / RBE2 等）；
3. **焊接 / 紧固件连接**（CWELD / CFAST 等）。

---

## 四、【可选，优先级最低】锦上添花

时间充裕再考虑，不影响标书完整性：质量单元 CMASS1/CONM1、粘性阻尼 CVISC/CDAMP、各向异性材料 MAT2/MAT8、约束 SPCD/MPC 各补一个最小算例。

---
